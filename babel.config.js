module.exports = {
    presets: [
        '@babel/preset-env',
        '@babel/preset-react',
        'babel-preset-react-native', // For React Native components
    ],
    plugins: [
        'react-native-web',
    ],
};
